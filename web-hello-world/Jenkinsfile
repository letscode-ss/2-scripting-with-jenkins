pipeline {
    agent { label 'master' }
    stages {
        stage("gradle build") { 
            steps {
                script { 
                    sh "gradle build"
                }
            }
        }
    }
}
