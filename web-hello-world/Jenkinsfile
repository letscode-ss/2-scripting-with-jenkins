pipeline {
    agent { label 'master' }
    stages {
        stage("gradle build") { 
            steps {
                script { 
                    sh "cd web-hello-world && gradle build"
                }
            }
        }
    }
}
